@PACKAGE_INIT@ 

set(RELY_LINK_DIR "@RELY_LINK_DIR@")
link_directories(${RELY_LINK_DIR})

set_and_check(aris_sim_INCLUDE_DIRS "@aris_sim_INCLUDE_DIRS@")
set(aris_sim_LIBRARIES "@aris_sim_LIBRARIES@")
if(UNIX)
	set(aris_sim_LIBRARIES -Wl,--start-group ${aris_sim_LIBRARIES} -Wl,--end-group)
endif(UNIX)


include("${CMAKE_CURRENT_LIST_DIR}/aris_sim-targets-release.cmake")
include("${CMAKE_CURRENT_LIST_DIR}/aris_sim-targets-debug.cmake")
